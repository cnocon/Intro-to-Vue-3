const app = Vue.createApp({
  data() {
    return {
      product: 'Socks',
      description: 'Vue is REACTIVE: When a data value changes, anywhere relying on that data will automatically update for us. We don’t have to do anything to make that happen.'
    }
  },
});
